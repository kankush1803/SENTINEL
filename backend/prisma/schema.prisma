// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  name      String
  role      String   // GUEST, STAFF, MANAGER, RESPONDER
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  incidents Incident[] @relation("ReportedBy")
}

model Incident {
  id          String   @id @default(uuid())
  type        String   // FIRE, MEDICAL, SECURITY, EVACUATION, SOS
  status      String   @default("OPEN") // OPEN, IN_PROGRESS, RESOLVED, CLOSED
  severity    String   @default("MEDIUM") // LOW, MEDIUM, HIGH, CRITICAL
  location    String
  description String?
  metadata    String?  // JSON string for additional AI data
  reportedBy  User?    @relation("ReportedBy", fields: [reporterId], references: [id])
  reporterId  String?
  zone        Zone?    @relation(fields: [zoneId], references: [id])
  zoneId      String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Venue {
  id        String   @id @default(uuid())
  name      String
  floors    Floor[]
  createdAt DateTime @default(now())
}

model Floor {
  id        String   @id @default(uuid())
  level     Int
  name      String
  venueId   String
  venue     Venue    @relation(fields: [venueId], references: [id])
  zones     Zone[]
  createdAt DateTime @default(now())
}

model Zone {
  id          String     @id @default(uuid())
  name        String
  description String?
  floorId     String
  floor       Floor      @relation(fields: [floorId], references: [id])
  incidents   Incident[]
  createdAt   DateTime   @default(now())
}

model Log {
  id        String   @id @default(uuid())
  action    String
  details   String
  userId    String?
  createdAt DateTime @default(now())
}
